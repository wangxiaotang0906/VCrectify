# Observed local acceptance environment, 2026-09-18.
# Windows AMD64, CPython 3.9.13 (Anaconda). NOT a cross-platform lockfile.
# This records direct dependencies used in tested paths, not all transitive packages.
# General supported constraints and optional install groups live in pyproject.toml.
# Install into a separate environment; do not apply this snapshot to unrelated work.

# Core preprocessing, framework and HTTP adapter dependencies.
numpy==1.24.4
scipy==1.9.1
h5py==3.7.0
PyYAML==6.0
requests==2.28.1

# Test runner used for the local verification suite.
pytest==7.1.2
# On this restricted Windows host tests also set PYTHONDONTWRITEBYTECODE=1.

# Optional official TxPert adapter: validated local versions, intentionally
# commented so core installation does not silently install CUDA/native wheels.
# Choose matching PyTorch/CUDA/PyG wheel indexes for YOUR platform before enabling.
# torch==2.5.1+cu124
# torchvision==0.20.1+cu124
# torch-geometric==2.6.1
# torch-scatter==2.1.2+pt25cu124
# omegaconf==2.3.0
# antlr4-python3-runtime==4.9.3
# timm==1.0.15
# pandas==1.4.4
# networkx==3.2.1
# typing-extensions==4.15.0
# aiohttp==3.13.5
# Selected optional wheels were installed in .runtime/txpert_deps; the adapter
# accepts dependency_paths rather than requiring modification of the base Python.
# Upstream TxPert declares a different environment (Python 3.12/PyTorch 2.6);
# this snapshot records observed adapter compatibility, not upstream support.

# Optional local Hugging Face client:
# transformers / accelerate / safetensors were not installed in this inspected
# Python environment and therefore have no claimed tested pin here. See [llm]
# in pyproject.toml for declared dependency constraints. The HTTP JSON client
# uses Python's standard library; the external inference server/model has its
# own separately recorded version and license.
# The Qwen3-8B service URL remains intentionally unset; HTTP transport fixtures
# validate integration contracts, not real language-model inference quality.
